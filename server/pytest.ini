[pytest]
# Run from the server/ directory: `cd server && pytest`
pythonpath = .
testpaths = tests
addopts = --strict-markers
markers =
    integration: requires a live PostgreSQL; set CRYO_TEST_DSN to enable
