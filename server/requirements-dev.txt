# Test/dev dependencies for the server. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
httpx>=0.27      # required by fastapi.testclient.TestClient
